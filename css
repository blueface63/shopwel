/* Global Styles */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    background-image: url('images/background.jpg');
    background-size: cover;
    color: #333;
}

header {
    background: rgba(0, 0, 0, 0.8);
    color: #fff;
    padding: 1em;
    text-align: center;
}

nav {
    display: flex;
    justify-content: center;
    background: #444;
}

nav a {
    color: white;
    padding: 15px;
    text-decoration: none;
}

nav a:hover {
    background: #666;
}

/* Home Page */
.home-content {
    text-align: center;
    padding: 2em;
}

.home-gallery {
    display: flex;
    justify-content: center;
    gap: 10px;
}

.home-gallery img {
    width: 200px;
    height: auto;
}

/* Pages (Clothes and Shoes) */
.page-content {
    padding: 2em;
    text-align: center;
}

.page-gallery {
    display: flex;
    justify-content: center;
    gap: 10px;
    flex-wrap: wrap;
}

.page-gallery img {
    width: 150px;
    height: auto;
    margin: 10px;
}